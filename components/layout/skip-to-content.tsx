/** Keyboard-only skip link — hidden until Tab focus (not visible on normal page load). */
export function SkipToContent() {
  return (
    <a href="#main-content" className="skip-link">
      Skip to content
    </a>
  );
}
